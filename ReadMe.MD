install:

```bash
npm install
```

preview on localhost:8080:

```bash
npm start
```

build to ./build directory:

```bash
npm run build
```

deploy gh-pages:

```bash
npm run deploy
```


[DEMO](https://mrjonas.github.io/docs_template/)
