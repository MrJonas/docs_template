{
  "dependencies": {
    "moment": "2.3.x",
    "typogr": "0.5.x",
    "underscore": "1.4.x",
    "wintersmith": "2.3.1",
    "wintersmith-sassy": "^1.0.0"
  },
  "private": true,
  "scripts": {
    "build": "wintersmith build -C .",
    "clean": "rimraf build",
    "start": "wintersmith preview -C .",
    "deploy": "gh-pages -d build",
    "build:deploy": "npm run clean && npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "rimraf": "^2.6.1"
  }
}
